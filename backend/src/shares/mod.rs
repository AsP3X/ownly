// Human: Public share links — create/revoke (auth) and scoped anonymous read/download.
// Agent: MODULE shares::handlers + shares::store; ROUTES under /api/v1/shares and /api/v1/public/shares.

pub mod handlers;
pub mod store;
